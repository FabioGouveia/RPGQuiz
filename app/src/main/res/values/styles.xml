<resources>
    <!--forces the app to be fullscreen properly-->
    <style name="DungeonTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorButtonNormal">@color/colorPrimaryLight</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>


    <!-- Global styles -->
    <style name="LargeView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="LargeView.Vertical">
        <item name="android:orientation">vertical</item>
    </style>

    <style name="LargeView.Horizontal">
        <item name="android:orientation">horizontal</item>
    </style>


    <!-- Progressbar horizontal customized styles -->
    <style name="LoadingProgressbar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/progressbar_height</item>
        <item name="android:max">@integer/progressbar_max</item>
        <item name="android:progressDrawable">@drawable/bgd_progressbar</item>
    </style>

    <style name="LoadingProgressbar.ThinBar">
        <item name="android:layout_height">@dimen/progressbar_thin_height</item>
    </style>

</resources>
